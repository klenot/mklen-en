import SinglePageExperience from "app/components/Homepage/single-page-experience";

export const metadata = {
  title: "Marek Klenotic — Digital growth & operations",
  description:
    "Single-page site structured like the arch.agency benchmark: DM Sans, deep blue palette, reel-led hero, case carousel, process slider, and client wall.",
};

export default function Home() {
  return <SinglePageExperience />;
}
